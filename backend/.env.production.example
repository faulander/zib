# Production Environment Variables for Zib RSS Reader
# 
# INSTRUCTIONS FOR PRODUCTION DEPLOYMENT:
# 1. Copy this file to .env: cp .env.production.example .env
# 2. Replace the example values below with your actual production domain
# 3. The .env file will be ignored by git for security
#
# FOR LOCAL DEVELOPMENT:
# No .env file needed - docker-compose will work with defaults
#
# USAGE:
# - CORS_ORIGINS: Comma-separated list of domains allowed to access the API
# - FRONTEND_URL: The public domain where your frontend will be served  
# - API_BASE_URL: The public URL for API calls (usually your-domain.com/api)

# Backend CORS Settings - REQUIRED FOR PRODUCTION
CORS_ORIGINS=https://your-domain.com,http://your-domain.com

# Frontend URLs - REQUIRED FOR PRODUCTION
FRONTEND_URL=https://your-domain.com
API_BASE_URL=https://your-domain.com/api

# Optional: Database settings (uses default data/zib.db if not set)
# DATABASE_URL=sqlite:///data/zib.db

# Optional: Debug settings for production
# DEBUG=false
# LOG_LEVEL=INFO